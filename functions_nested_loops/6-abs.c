#include "main.h"

/**
 *_abs - compute absolute value of a int
 *@num: this will be the computed integer
 *Return: the absolute value of the int
 */

int _abs(int num)
{

	if (num < 0)
	{
		return (-num);
	}

	else
		return (num);
}
