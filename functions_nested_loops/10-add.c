#include "main.h"

/**
 *add - is a function that adds two integers and returns the result
 *@num1: store the first number to add
 *@num2: store the second number to add
 *Return: return the add result
 */

int add(int num1, int num2)
{
	int result;

	result = num1 + num2;

	return (result);
}
